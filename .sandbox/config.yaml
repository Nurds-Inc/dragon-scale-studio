# Sandbox configuration for Dragon Scale Studio
# Manual runtime for better control during development

runtime: manual
autostart: false
health_path: /

# Git sync configuration - auto-pull changes from GitHub
sync:
  enabled: true
  branch: main
  interval_seconds: 10
  rebase: true
